# AgentFront - 智能字幕助手前端

基于 React + Tauri 的现代化视频字幕编辑客户端，提供所见即所得的字幕编辑体验。

## ✨ 核心功能

- **🤖 自动识别**: 集成 Whisper 模型，一键生成高精度字幕
- **📝 可视化编辑**: 拖拽式布局，实时预览字幕效果
- **🎨 样式定制**: 完整的 ASS 样式支持（字体、颜色、描边、阴影等）
- **🔥 硬字幕烧录**: 支持将字幕永久烧录到视频中
- **🧠 AI Copilot**: 智能助手辅助润色、翻译和样式调整
- **🖥️ 跨平台**: 支持 Web 访问和 Windows/macOS/Linux 桌面应用

---

## 🏗 技术栈

| 模块 | 技术选型 | 说明 |
|------|----------|------|
| **核心框架** | React 18 + TypeScript | 强类型组件化开发 |
| **构建工具** | Vite | 极速开发服务器与构建 |
| **桌面封装** | Tauri (Rust) | 高性能、轻量级跨平台桌面运行时 |
| **UI 布局** | react-grid-layout | 灵活的可拖拽网格布局系统 |
| **样式方案** | Tailwind CSS | 原子化 CSS，快速构建界面 |
| **媒体处理** | @ffmpeg/ffmpeg (WASM) | 浏览器端轻量级音视频处理 |
| **状态管理** | Context API | 全局状态共享 (Auth, Theme 等) |

---

## 🚀 快速开始

### 环境要求
- Node.js 18+
- pnpm (推荐) 或 yarn
- Rust (仅开发桌面端需要)

### 启动步骤

1. **安装依赖**
   ```bash
   cd agentfront
   pnpm install
   ```

2. **启动 Web 开发服务器**
   ```bash
   pnpm dev
   ```
   访问 `http://localhost:5173`。
   > 注意：需要同时启动后端服务才能使用完整功能。

3. **启动桌面版 (Tauri)**
   确保已安装 Rust 环境。
   ```bash
   pnpm tauri dev
   ```

---

## 📁 目录结构

```
agentfront/
├── src/
│   ├── agents/          # AI 代理交互逻辑 (LangChain)
│   ├── components/      # UI 组件 (编辑器、预览、面板等)
│   ├── context/         # 全局状态 Context
│   ├── hooks/           # 自定义 Hooks (useVideoScale 等)
│   ├── pages/           # 页面组件
│   ├── tools/           # 前端工具函数
│   ├── types/           # TypeScript 类型定义
│   └── utils/           # 通用工具类
├── src-tauri/           # Tauri Rust 后端代码
├── public/              # 静态资源 (ffmpeg-core 等)
└── vite.config.ts       # Vite 配置 (含代理设置)
```

---

## 🔌 API 代理

前端开发环境通过 `vite.config.ts` 配置了 API 代理，将 `/api` 请求转发到后端服务（默认 `http://127.0.0.1:8000`）。

```typescript
// vite.config.ts
server: {
  proxy: {
    '/api': {
      target: 'http://127.0.0.1:8000',
      changeOrigin: true,
    },
    // ...
  }
}
```

---

## 🤝 贡献指南

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 提交 Pull Request

---

## 📄 许可证

MIT License


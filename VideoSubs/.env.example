
## Copy this file to `.env` and fill in your keys
## Rename: copy `.env.example` -> `.env` and fill values
## Example .env for VideoSubs

# Search tool (Tavily) API key
TAVILY_API_KEY=

# 通用 LLM Key（OpenAI 或兼容服务）。在代码中称为 LLM_API_KEY
LLM_API_KEY=

# 可选：LangGraph / LangSmith 监控或调试 key（如果使用）
LANGSMITH_API_KEY=

# 可选：指定模型名称（例如 qwen-turbo、gpt-4等）
LLM_MODEL_NAME=qwen-turbo

# 可选：覆盖 OpenAI 兼容 base URL（例如阿里/自托管兼容端点）
LLM_OPENAI_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1

# 可选：LLM 温度（默认 0）
LLM_TEMPERATURE=0

# Whisper 模型名称（large-v3 / medium / small 等）
WHISPER_MODEL=large-v3

# To separate your traces from other application
LANGSMITH_PROJECT=new-agent

# Add API keys for connecting to LLM providers, data sources, and other integrations here
